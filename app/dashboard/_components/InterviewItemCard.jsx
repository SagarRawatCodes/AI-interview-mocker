/*'use client'
import React from 'react';

function InterviewItemCard({ interview }) {
  console.log("Card interview object:", interview);

  if (!interview) {
    return <div>Empty interview</div>;
  }

  return (
    <div className='border shadow-sm rounded-lg p-3 mb-4'>
      <h2 className='text-lg font-bold'>{interview.jobPosition || 'No job position found'}</h2>
      <p className='text-sm text-gray-600'>Experience: {interview.jobExperience}</p>
    </div>
  );
}

export default InterviewItemCard;

*/